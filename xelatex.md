
---

> Teilweises/inkonsistentes TeX-Live-Update auf Arch →
> alte `xelatex.fmt` passt nicht zur neuen XeTeX-Version →
> Neuinstallation von `texlive-xetex` regeneriert Formate →
> Problem behoben

---

**Tipp für die Zukunft:**


```bash
sudo fmtutil-sys --all
```

oder direkt:

```bash
sudo pacman -S texlive-xetex
```

---

